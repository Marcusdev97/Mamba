import path from "node:path";import{fileURLToPath}from"node:url";import{featureMigrationPlan,migrateFeature}from"./lib/feature-migration.mjs";
export const DASHBOARD_AI_MIGRATION_VERSION=309,DASHBOARD_AI_CONFIRMATION="APPLY_DASHBOARD_AI_AUDITOR_V1";
const migrationPath=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../../campaign-app/migrations/309-dashboard-ai-auditor.sql"),requiredTables=["lead_audit_analyses","lead_audit_events"];
const config=(o={})=>({...o,migrationPath:o.migrationPath||migrationPath,version:309,name:"dashboard-ai-auditor",prerequisite:308,requiredTables,confirmationToken:DASHBOARD_AI_CONFIRMATION,metadataKey:"dashboard_ai_auditor_schema"});
export const dashboardAiMigrationPlan=(o)=>featureMigrationPlan(config(o));export const migrateDashboardAiAuditor=(o)=>migrateFeature(config(o));
function args(a){const o={apply:false,confirmation:"",root:"",db:""};for(let i=0;i<a.length;i++){if(a[i]==="--apply")o.apply=true;else if(a[i]==="--dry-run")o.apply=false;else if(a[i]==="--confirm")o.confirmation=a[++i]||"";else if(a[i]==="--root")o.root=a[++i]||"";else if(a[i]==="--db")o.db=a[++i]||"";else throw new Error(`不支持的参数：${a[i]}`)}return o}if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){const a=args(process.argv.slice(2)),rootDir=path.resolve(a.root||path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../..")),databasePath=path.resolve(a.db||path.join(rootDir,"campaign-data/mamba.sqlite"));try{console.log(JSON.stringify(migrateDashboardAiAuditor({rootDir,databasePath,apply:a.apply,confirmation:a.confirmation}),null,2))}catch(e){if(e.report)console.error(JSON.stringify(e.report,null,2));console.error(`[${e.code||"DASHBOARD_AI_MIGRATION_FAILED"}] ${e.message}`);process.exitCode=1}}
